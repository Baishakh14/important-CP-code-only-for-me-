/*
Allaih is Almighty
Bismillahhi Rahmanir Rahim
Baishakh
*/
#include<bits/stdc++.h>
#include<ext/pb_ds/assoc_container.hpp>
#include<ext/pb_ds/tree_policy.hpp>
using namespace std;
using namespace __gnu_pbds;
typedef tree<int, null_type, less<int>, rb_tree_tag, tree_order_statistics_node_update> pbds;
#define ll long long
const int mod = 1e9+7;
// s.order_of_key(x) -> values total are small than x
// *s.find_by_order(x)  -> kth small elemnt (index) in pbds
void ss(vector<ll>&prime,ll l,ll r)
{
    ll hi = sqrt(r)+1;
    vector<ll>p(hi+1,1);
    p[0] = p[1] = 0;
    for(ll i = 2;i<=hi;i++)
    {
        if(p[i] == 1)
        {
            for(ll j = i*i;j<=hi;j+=i) p[j] = 0;
            for(ll j = max(i*i,(l+i-1)/i * i);j<=r;j+=i) prime[j-l] = 0;
        }
    }
}
int main()
{
    ll l,r;
    cin>>l>>r;
    vector<ll>prime(r-l+1,1);
    ss(prime,l,r);
  return 0;
}


//Tata Goodbye Khatam
