#include <iostream>
#include <vector>

using namespace std;

void computeETF(int n, vector<int> &phi) {
    for (int i = 1; i <= n; i++) {
        phi[i] = i;  // Initialize phi[i] = i
    }
    
    for (int i = 2; i <= n; i++) {
        if (phi[i] == i) {  // i is prime
            for (int j = i; j <= n; j += i) {
                phi[j] = (phi[j] * (i - 1)) / i;
            }
        }
    }
}

int main() {
    int n = 1000;
    vector<int> phi(n + 1);
    computeETF(n, phi);

    for (int i = 1; i <= n; i++) {
        cout << "ETF(" << i << ") = " << phi[i] << "\n";
    }
    
    return 0;
}
